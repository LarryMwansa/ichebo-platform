Attached Element Context from Integrated Browser

Element: img

URL: https://app.ichebo.org/

HTML Path: div#ics-shell.ics-shell > main#main-stage.ics-stage > header.ics-stage__topbar > div > a.ws-sidebar__avatar > img

Outer HTML:
```html
<img src="http://localhost:9000/ics-media/avatars/279459804_5424259184251579_9120027589424686071_n.jpg" alt="Larry Mwansa">
```

Dimensions:
- top: 12px
- left: 942px
- width: 32px
- height: 32px

CSS:
```css
*, *::before, *::after { padding: 0;
  margin: 0;
  box-sizing: border-box;
  list-style: none;
  list-style-type: none;
  text-decoration: none;
  -webkit-font-smoothing: antialiased;
  -moz-osx-font-smoothing: grayscale;
  text-rendering: optimizeLegibility; }
img, svg, picture { display: block;
  max-width: 100%;
  height: auto;
  object-fit: cover;
  background-size: cover;
  background-repeat: no-repeat;
  image-rendering: -webkit-optimize-contrast;
  image-rendering: -moz-crisp-edges;
  image-rendering: crisp-edges; }
.ws-sidebar__avatar img { width: 100%;
  height: 100%;
  object-fit: cover; }

/* Inherited */
*, *::before, *::after { list-style: none; list-style-type: none }
a, button { cursor: pointer; white-space: nowrap; color: inherit }
.ws-sidebar__avatar { font-size: 13px; font-weight: 700; color: #fff }
.ws-sidebar__avatar { font-weight: 700; color: var(--text) }
html, body { font-family: var(--font-family-base); line-height: var(--line-height-base); font-weight: var(--font-weight-regular) }
body { font-family: var(--ff-body); font-size: 16px; line-height: 1.5; color: var(--text) }
html { font-size: 100%; font-style: normal }
:root { color: var(--text) }

/* Resolved values */
margin: 0px;
padding: 0px;
text-decoration: rgb(245, 243, 240);
-webkit-font-smoothing: antialiased;
background-repeat: no-repeat;
background-size: cover;
box-sizing: border-box;
color: rgb(245, 243, 240);
cursor: pointer;
display: block;
font-family: Inter, system-ui, sans-serif;
font-size: 13px;
font-style: normal;
font-weight: 700;
height: 30.4px;
image-rendering: crisp-edges;
line-height: 19.5px;
list-style-image: none;
list-style-position: outside;
list-style-type: none;
max-width: 100%;
object-fit: cover;
overflow-clip-margin: content-box /*UA*/;
overflow-x: clip /*UA*/;
overflow-y: clip /*UA*/;
text-rendering: optimizelegibility;
width: 30.4px;

/* CSS variables */
--line-height-base: 1.6;
--font-family-base: 'Inter', system-ui, sans-serif;
--font-weight-regular: 400;
--text: #f5f3f0;
--ff-body: 'Inter', system-ui, sans-serif;
```